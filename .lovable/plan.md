

# Убрать курсив, убрать фон кнопок, сделать тёмный текст

## Проблема
1. Описания отображаются курсивом (`italic`) — плохо читается
2. Кнопки имеют тёмный полупрозрачный фон (`::after` с `rgba(20,10,40,0.4)`) — нужно убрать
3. Текст кнопок белый — нужно сделать тёмным

## Изменения

### 1. `src/components/MagicBook.tsx` — убрать `italic`
- Строка 139: убрать `italic` из placeholder
- Строка 156: убрать `italic` из описания записи
- Строка 174: убрать `italic` из live-preview описания

### 2. `src/index.css` — кнопки
- Строка 134: `color: #fff` → `color: hsl(235 30% 20%)` (тёмно-синий, как чернила)
- Строка 142: убрать `text-shadow` (белое свечение не нужно для тёмного текста)
- Удалить `::after` блок (строки 159-166) — убрать тёмный фон внутри кнопки
- Удалить `hover::after` (строки 177-179)
- Оставить `::before` (радужная рамка + свечение) как есть

