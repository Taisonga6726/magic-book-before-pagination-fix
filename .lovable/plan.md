

# Plan: Расширить описание на всю ширину и уменьшить межстрочный интервал

## Проблема
На скриншоте видно: описание начинается с отступом и не заполняет всю ширину между орнаментами. Межстрочный интервал слишком большой.

## Изменения

### `src/components/MagicBook.tsx`

**Контейнер правой страницы (line 336):**
- `left: "54%"` → `"52%"` — сдвинуть левее, ближе к корешку
- `width: "23%"` → `"25%"` — расширить до правого орнамента

**Описание записей (line 363):**
- `text-lg` → `text-base` (чуть мельче)
- Добавить `lineHeight: "1.15"` — уменьшить межстрочный интервал

**Live-preview описание (line 380):**
- Те же изменения: `text-base`, `lineHeight: "1.15"`

**Отступы между записями (line 349):**
- `space-y-2` → `space-y-1` — меньше отступ между записями

### `src/components/FinalBook.tsx`

**Описание (line 100):**
- Добавить `lineHeight: "1.15"` для консистентности с MagicBook

