

# Подпись ярче, убрать курсив + слова меньше и без розового

## Изменения

### 1. `src/components/MagicBook.tsx` (строки 187-196) — подпись ярче, без курсива
- Заменить текстовую подпись на изображение `podpi_s.jpg` (золотая подпись с чёрным фоном)
- Скопировать `user-uploads://podpi_s.jpg` в `src/assets/podpis.jpg`
- Вместо `<span>` использовать `<img>` с `mix-blend-mode: screen` (чёрный фон станет прозрачным)
- Размер ~150-180px по ширине, позиция `bottom: 5%, right: 10%`

### 2. `src/components/FloatingWords.tsx` — слова меньше, убрать розовый
- **Размеры**: уменьшить все значения в `SIZES` на ~30% (например 24→16, 30→20, 34→22, 36→24 и т.д.)
- **Убрать розовый цвет**: заменить все `color: "pink"` на `color: "purple"` или `color: "blue"` (чередуя)
- Убрать использование `glow-text-pink` — только `glow-text-purple` и `glow-text-blue`

