
# Plan: Выровнять левую страницу FinalBook

Изменить только контейнер левой страницы в `src/components/FinalBook.tsx`:

- `left: "20%"` → `"22%"`
- `width: "26%"` → `"24%"`
- `padding: "12px 4px 40px 2px"` → `"12px 8px 40px 12px"`

Правая страница, логика, пагинация и renderEntry — без изменений.
