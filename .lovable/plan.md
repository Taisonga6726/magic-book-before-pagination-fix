

# Plan: Вписать текст FinalBook в орнамент — сдвинуть ниже и правее

## Проблема
На скриншоте видно: текст на левой странице залезает на верхний орнамент и выходит за левый край. Нужно сдвинуть вниз и вправо, уменьшить область — точно как делали для MagicBook.

## Сравнение с MagicBook

MagicBook (правая страница):
- `left: "54%"`, `top: "18%"`, `width: "26%"`, `height: "60%"`
- `padding: "12px 2px 40px 4px"`

## Изменения в `src/components/FinalBook.tsx`

### Левая страница (line 177)
Сдвинуть вниз и правее, уменьшить область чтобы вписаться в орнамент:
- `left: "20%"` → `"21%"` (правее, от левого орнамента)
- `top: "18%"` → `"22%"` (ниже, от верхнего орнамента)  
- `width: "26%"` → `"25%"` (чуть уже, не залезать на орнамент)
- `height: "60%"` → `"54%"` (короче, не залезать на нижний орнамент)
- `padding: "12px 4px 40px 2px"` → `"8px 4px 32px 2px"`

### Правая страница (line 192)
Аналогично привести в соответствие:
- `top: "18%"` → `"22%"`
- `height: "60%"` → `"54%"`
- `padding: "12px 2px 40px 4px"` → `"8px 2px 32px 4px"`

### Пересчёт пагинации
В DOM-измерении (useEffect) обновить высоту контейнера — использовать тот же `54%` от высоты, чтобы записи корректно распределялись по страницам и не было пустого места.

