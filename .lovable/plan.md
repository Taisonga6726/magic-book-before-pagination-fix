

# Переключаем на Marck Script для сравнения

## Изменения

### `src/components/MagicBook.tsx`
- Слово: `font-book italic font-bold text-xl` → `text-xl` со стилем `fontFamily: "'Marck Script', cursive"` (убираем italic/bold — Marck Script сам по себе курсивный)
- Live preview: аналогично — Marck Script
- Нумерация: добавить `font-semibold` и поднять opacity до `0.65` для чёткости

Один файл, минимальные правки — только замена шрифта слов и усиление нумерации.

