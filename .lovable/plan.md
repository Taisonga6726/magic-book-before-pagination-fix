

# Plan: Выровнять записи по орнаменту — оба компонента

## Что нужно сделать
1. Номер + слово — убрать `ml-7` у описания, вместо этого описание на полную ширину от орнамента до орнамента
2. Описание — `textAlign: "justify"`, убрать отступ `ml-7`, уменьшить вертикальные отступы между абзацами
3. Применить ко всем страницам: FinalBook и MagicBook

## Изменения

### `src/components/FinalBook.tsx`

**renderEntry (line 94-109):**
- Убрать `mb-0.5` → `mb-0`
- Описание (line 100): убрать лишние отступы, оставить `mt-0`, `leading-snug`, `textAlign: "justify"` (уже есть)
- Контейнер уже без `ml-7` — ОК

**Левая страница (line 141):** `left: "29%"`, `width: "20%"` — расширить до `width: "21%"`, `left: "28%"` чтобы текст заполнял от орнамента до орнамента (корешок).

**Правая страница (line 156):** `left: "51%"`, `width: "23%"` — расширить до `width: "24%"` чтобы заполнить до правого орнамента.

### `src/components/MagicBook.tsx`

**Правая страница entries (line 363):** убрать `ml-7` у описания — текст описания от орнамента до орнамента.

**Live-preview описание (line 380):** тоже убрать `ml-7`.

**Контейнер правой страницы (line 336):** `width: "25%"` — расширить до `width: "26%"` для заполнения до орнамента.

## Итог
- Описание занимает всю ширину от орнамента до орнамента (justify)
- Отступы между записями минимальны
- Номер + слово на одной строке, описание сразу под ним без лишнего отступа слева

