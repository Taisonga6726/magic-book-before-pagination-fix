

# План: удаление подписи + правки вёрстки книги

## 1. Удалить подпись (podpis-clean.png)

**MagicBook.tsx** (строки 325-334): удалить блок `<img src="/src/assets/podpis-clean.png" ...>`

**FinalBook.tsx** (строки 222-231): удалить аналогичный блок

## 2. Расширить зону ввода (левая страница)

**MagicBook.tsx** (строка 198):
- `left: "18%"` → `left: "12%"`
- `width: "22%"` → `width: "36%"` (до переплёта ~50%)
- padding: `"16px 24px 12px 32px"` → `"12px 16px 10px 28px"`

**FinalBook.tsx** (строка 143): аналогичные изменения для левой страницы

## 3. Расширить правую страницу

**MagicBook.tsx** (строка 232):
- `left: "52%"` → `left: "52%"` (без изменений — начало от переплёта)
- `width: "32%"` → `width: "36%"` (расширить вправо)
- padding: `"16px 28px 12px 24px"` → `"12px 20px 10px 20px"`

**FinalBook.tsx** (строка 165): аналогично

## 4. Уменьшить межстрочные интервалы

В обоих компонентах:
- `space-y-4` → `space-y-2` (записи плотнее)
- Описание: `mt-0.5` оставить
- `line-height` для textarea: `32px` → `28px`

## 5. Кнопка «далее →» — видимость

**MagicBook.tsx** (строка 289):
- `bottom-[15%]` → `bottom-[18%]` (выше орнамента)
- `text-xl` → `text-2xl`
- Добавить `font-bold`

## 6. «сохранить | редактировать» — поднять выше

**MagicBook.tsx** (строка 221):
- `mt-3` → `mt-1` (поднять от нижнего орнамента)

## 7. «завершить книгу» — поднять

**MagicBook.tsx** (строка 301):
- `bottom: "10%"/"12%"` → `bottom: "14%"/"16%"`

## Что НЕ меняется
- Цвета, логика, анимации, звуки, FloatingWords

