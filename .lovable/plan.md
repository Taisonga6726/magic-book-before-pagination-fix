

# Plan: Сдвинуть правую страницу ещё правее + уменьшить размер заголовка

## Проблема
На скриншоте: цифра "25." всё ещё попадает на орнамент. Длинная фраза "26. В прод случайно — и оно работает!!!" не помещается в одну строку из-за крупного шрифта.

## Изменения в `src/components/MagicBook.tsx`

### 1. Контейнер правой страницы (line 336)
- `left: "52%"` → `"54%"` — сдвинуть правее, чтобы цифра не попадала на орнамент
- `width: "25%"` → `"23%"` — правый край остаётся на месте (54+23=77)

### 2. Размер заголовка (lines 356, 370)
- `text-2xl` → `text-xl` — уменьшить шрифт заголовка, чтобы длинные фразы помещались в одну строку

Больше ничего не меняю — описания, justify, lineHeight остаются как есть.

