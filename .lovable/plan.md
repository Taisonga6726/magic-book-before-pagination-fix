

## Цель
Удалить кнопку «📤 поделиться» с финального экрана.

## Файл: `src/components/ControlBar.tsx`
- В блоке `mode === "final"` удалить строку с `<NeonGlassButton onClick={onShare}>📤 поделиться</NeonGlassButton>`.
- Оставить только `📖 к книге`.
- Проп `onShare` оставляем в интерфейсе (логика не трогается, передаётся из родителя — без вреда).

## Что НЕ трогаем
- Логику, остальные режимы, стили, layout.

## Критерии приёмки
- На финальном экране только одна кнопка «📖 к книге».
- Кнопка «поделиться» отсутствует.

