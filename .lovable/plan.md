

# Plan: Расширить описание до правого орнамента

## Проблема
На скриншоте видно: описание (текст после "—") не доходит до правого орнамента — есть большой зазор справа. Стрелки указывают на пустое пространство.

## Изменения в `src/components/MagicBook.tsx`

### Контейнер правой страницы (line 336)
- `width: "23%"` → `"26%"` — расширить контейнер вправо до орнамента
- `padding: "12px 8px 40px 4px"` → `"12px 2px 40px 4px"` — уменьшить правый padding, чтобы текст заполнял максимум ширины

Итого: left 54% + width 26% = 80%, padding-right 2px — текст будет доходить до правого орнамента.

