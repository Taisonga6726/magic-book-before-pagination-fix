

# Plan: Слово на полную ширину + межстрочный для длинных фраз

## Проблема
На скриншоте: цифра+слово начинается слишком левее, длинные фразы (как "26 В прод случайно — и оно работает!!!") не выравнены по ширине и имеют большой межстрочный интервал.

## Изменения

### `src/components/MagicBook.tsx`

**Номер + слово (lines 355-361, 372-377):**
- Убрать `flex items-baseline gap-1` — заменить на обычный `div`
- Номер и слово inline, вся строка `textAlign: "justify"`, `lineHeight: "1.15"`
- Это позволит длинным фразам переноситься и заполнять ширину

**Live-preview (lines 372-377):** аналогично.

### `src/components/FinalBook.tsx`

**renderEntry (lines 95-97):**
- Тоже убрать flex, сделать inline номер+слово с `lineHeight: "1.15"`

### Правило (сохранить в память)
Все длинные фразы (и заголовок, и описание) — `textAlign: "justify"`, `lineHeight: "1.15"`, заполняют ширину от орнамента до орнамента.

