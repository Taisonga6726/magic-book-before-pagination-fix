

# Ограничение текста по нижнему орнаменту

## Текущее состояние
Все пункты плана (1-7) уже применены в коде. Единственное, что не сделано — защита текста от наложения на нижний орнамент.

## Что нужно сделать

### MagicBook.tsx
- Левая страница (строка 198): увеличить нижний padding с `10px` на `40px` — `"12px 16px 40px 28px"`
- Правая страница (строка 232): аналогично — `"12px 20px 40px 20px"`
- Добавить `overflow: "hidden"` на левую страницу (если нет)

### FinalBook.tsx
- Левая страница (строка 143): padding → `"12px 16px 40px 28px"`
- Правая страница (строка 165): padding → `"12px 20px 40px 20px"`

Это создаст "мёртвую зону" в 40px снизу, куда текст не зайдёт — орнамент останется чистым.

